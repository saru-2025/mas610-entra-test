name: 🚀 Deploy to Test ADF & Databricks

on:
  workflow_dispatch:

jobs:
  deploy-test:
    runs-on: ubuntu-latest
    environment: test

    steps:
      - name: 🧩 Checkout Test repo
        uses: actions/checkout@v4

      - name: 🔐 Azure Login
        uses: azure/login@v1
        with:
          creds:'{
              "clientId":"${{ secrets.SP-CLIENT-ID-TEST}}",
              "clientSecret":"${{ secrets.SP-CLIENT-SECRET-TEST}}",
              "tenantId":"${{ secrets.SP-TENANT-ID-TEST}}",
              "subscriptionId":"${{ secrets.Subcription-id}}"
            }'

      # ✅ Deploy ADF template using ARM Deploy Action
      - name: 🚀 Deploy Azure Data Factory ARM Template
        uses: Azure/arm-deploy@v1
        with:
          scope: resourcegroup
          resourceGroupName: rg_data_platform
          template: factory/adf-data-platform-test.json
          parameters: factory/arm-template-parameters.json
          deploymentName: adf-test-deployment
          failOnStdErr: false

      # ⚙️ Install Databricks CLI
      - name: ⚙️ Install Databricks CLI
        run: pip install databricks-cli

      # 🔑 Configure Databricks CLI
      - name: 🔑 Configure Databricks
        run: |
          databricks configure --token <<EOF
          ${{ secrets.DATABRICKS_HOST }}
          ${{ secrets.DATABRICKS_TOKEN }}
          EOF

      # 📦 Upload Notebooks
      - name: 📦 Upload Notebooks to Test Workspace
        run: |
          echo "Uploading notebooks to Test workspace..."
          databricks workspace import_dir notebooks /Shared/Test_Notebooks --overwrite
